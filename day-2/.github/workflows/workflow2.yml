name: creating new pipeline 
on: push
jobs: 
    event-info-job:
         runs-on: ubuntu-latest
         steps:
              - name: Step1 
                run: echo "my Trigger event is ${{ github.event_name }} event "
                